# Secure Bank Management System (C Language)

A backend logic project demonstrating core computer science concepts using C. This console application manages user bank accounts with persistent data storage.

## 🛠 Technical Concepts
* **File Handling:** Uses `fopen()`, `fwrite()`, and `fread()` to simulate a database using text files.
* **Structs & Pointers:** Efficient memory management for handling customer records.
* **CRUD Operations:** Implements logic to Create, Read, Update (Deposit/Withdraw) user data.

## ⚙️ How to Run
1.  Clone the repository.
2.  Compile using GCC: `gcc main.c -o bank`
3.  Run the executable: `./bank`
